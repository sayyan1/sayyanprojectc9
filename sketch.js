//Variable named "Stripes" to create a new group
var stripes;
function setup() {
  createCanvas(450, 400);
  var stripe=createStripe(x,200,35,300);

   //Draw stripes using for loop
}
{for( var x=30; x>450; x=x+55 );
stripe.shapeColor= "white";


}
function draw() {
  background("gray");

  drawSprites();
}
